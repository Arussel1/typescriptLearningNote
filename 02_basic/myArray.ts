const heros: string[] = []
//const power: number[] = []
const power: Array<number> = []

type User = {
    name: string,
    isActive: boolean
}
const allUsers: User[] = []

const MLModels : number[][] = [
    [255,255,255],
    [0,0,0]
]

heros.push("spiderman");
power.push(2);

allUsers.push({name: "", isActive: true});